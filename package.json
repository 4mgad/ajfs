{
	"name": "ajfs",
	"version": "3.0.0",
	"description": "ajfs is a module that extends Node's default File System (fs) module to recursively copy/delete/traverse directories with callbacks to notify the caller upon each successful copy/delete/encounter of a file or directory and upon completion of the whole process.",
	"author": "AJ <aj@amgad.us>",
	"main": "index.js",
	"repository": {
		"type": "git",
		"url": "https://github.com/4mgad/ajfs.git"
	},
	"bugs": {
		"url": "https://github.com/4mgad/ajfs/issues"
	},
	"keywords": [
		"fs",
		"file system",
		"directory",
		"file",
		"copy",
		"delete",
		"util",
		"utility"
	],
	"engines": {
		"node": "8.1.*",
		"npm": "5.0.*"
	},
	"scripts": {
		"build": "babel src --out-file index.js",
		"test": "npm run build && mocha"
	},
	"devDependencies": {
		"babel-cli": "^6.24.1",
		"babel-preset-env": "^1.5.2",
		"mocha": "^3.4.2"
	}
}
